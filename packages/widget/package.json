{
  "name": "@exp/widget",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    "./types": "./src/types.ts"
  },
  "scripts": {
    "build": "tsc --noEmit && vite build && vite build --mode loader",
    "dev": "vite build --mode loader && vite build --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@zumer/snapdom": "^2.12.8",
    "preact": "^10.29.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.5",
    "happy-dom": "^20.10.2",
    "typescript": "^6.0.3",
    "vite": "7.3.3",
    "vitest": "4.1.5"
  }
}
