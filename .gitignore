node_modules
.DS_Store
.idea
dist
dist-ssr
*.local
count.txt
.env
.claude
.nitro
.tanstack
.output
.vinxi
count.txt
Caddyfile
test-results
recordings

# Internal strategy/marketing docs — kept locally, not part of the public repo (EXP-286)
docs/masterplan.md
docs/vision.md
docs/marketing-playbook.md
docs/archive/
docs/reviews/
apps/marketing/movie-ref/

# Rust (apps/desktop)
/apps/desktop/target
/apps/desktop/target-*/

# Built widget artifacts (emitted by packages/widget into the web public dir)
apps/web/public/widget/

# Raw capture output for the screenshot store (EXP-566) — only the optimized
# shots/**.webp are committed; regenerate with `bun run shots`.
/.shots-raw/
