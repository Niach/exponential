- Workflows: a chip strip picks a node and opens it into Issue, Run, Changes and Results, a question the planner asks shows in a banner, Pause and Dismiss sit in the overflow, and the final pull request lists under Reviews where it merges
- Agent runs: a workflow's runs group under their node with a status dot and caption, review runs carry their titles, a run that needs you wears a dot, and the Recent sheet draws the tree
- Issues: the blocks badge opens a mini-graph drawn on one shared grid, and the header of a stacked or batch run shows the front issue with a count
- Editor: typing --- inserts a separator at once
- Activity: estimate changes fold like every other field edit
