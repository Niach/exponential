• Merging a PR no longer ends the coding session — it keeps running until you explicitly close it
• New "Merge and close" button on live session rows for one-tap merge + teardown
• Session status now shows "Merged" while a session outlives its merged PR
