- Reviews: every pull request row carries one control, Merge or Merge stack at the bottom of a stack, and a workflow node's pull request says it merges through the workflow
- Workflows: Open final PR appears when a workflow's final pull request was closed, the event log sits under the graph and a picked node shows only its own events, and failed or waiting states read amber
- Agent runs: a run waiting on your answer wears a red dot, and a workflow run on another account says which one
- Starting an issue that already has a live run says so instead of starting a second one
