• Multi-select refinements — picking several issues at once is faster and more predictable, and bulk edits now apply in one go
• Reworked review screen — merging and closing a pull request moved into a bottom action bar, and big diffs open collapsed so they load instantly
• Steering that reconnects — the live agent terminal recovers from a dropped connection on its own instead of stranding you on a dead screen
• Issue list alignment — identifiers line up in My Issues and search the way they do on a board
• Various stability and consistency fixes
