# screengrab configuration for automated Play Store screenshots.
#
# Prereqs (see StoreScreenshotsTest.kt):
#   1. A running, seeded local backend reachable from the emulator:
#        bun run backend:up && bun dev
#        bun run seed:screenshots                                    (from apps/web)
#   2. Live steering genuinely switched on — three of the eight shots need it,
#      and without it the issue detail reads "Live steering is unavailable on
#      this instance" (EXP-393):
#        docker compose --profile steer up -d                        (repo root)
#        export STEER_RELAY_URL=ws://localhost:4002
#        # …plus the matching STEER_RELAY_SECRET, then in a second shell:
#        bun run screenshots:desktop                                 (from apps/web)
#      Leave it running for the whole capture. The steering shot gates on the
#      published transcript, so a wrong URL fails the run instead of quietly
#      photographing a "Reconnecting…" screen.
#
#      EXP-812: the server keeps `localhost` — it is BOTH the ws:// URL it mints
#      for clients and the http:// base it calls itself for device presence, and
#      the browser lanes need it (Chromium blocks a ws://<LAN-IP> socket from the
#      https capture page as mixed content). The emulator cannot resolve it, so
#      the suite rewrites the AUTHORITY of the minted dial URL to the host alias
#      ws://10.0.2.2:4002 instead. Point it elsewhere with
#      SCREENGRAB_STEER_RELAY_URL; the rewrite is skipped entirely when
#      SCREENGRAB_INSTANCE_URL names a non-local backend.
#   3. An English-locale phone emulator booted (only one device attached).
#   4. Both APKs built: bundle exec fastlane screenshots does this for you.
#
# Output lands in fastlane/screenshots-raw/en-US/images/phoneScreenshots/
# (1_board.png … 8_support.png) — RAW device captures, not the Play upload set.
# Then run `bun run screenshots:store` from the repo root BEFORE `sync_store`:
# it composites the raw captures into the headline + bezel marketing panels
# (EXP-580) and writes the finished set to
# fastlane/metadata/android/en-US/images/phoneScreenshots/, which is what
# `supply` uploads to the Play listing.

# The production flavor's applicationId (debug build keeps the same id).
app_package_name("at.exponential")
tests_package_name("at.exponential.test")

# Only run the STORE screenshot flow, not any other instrumentation test — in
# particular not StyleguideScreenshotsTest, which has its own config file
# (Screengrabfile-styleguide) and its own lane.
use_tests_in_classes(["com.exponential.app.StoreScreenshotsTest"])

locales(["en-US"])

# Wipe the old screenshots for the captured locales before writing new ones.
clear_previous_screenshots(true)

# Stable filenames (1_board.png, …) so the Play metadata set stays diffable.
use_timestamp_suffix(false)

# Reinstall to clear app data between runs — the test drives the full
# first-launch flow (instance picker → login) and needs a signed-out app.
reinstall_app(true)

# productionDebug variant, built via `fastlane screenshots` (paths are relative
# to apps/android, where fastlane runs).
app_apk_path("app/build/outputs/apk/production/debug/app-production-debug.apk")
tests_apk_path("app/build/outputs/apk/androidTest/production/debug/app-production-debug-androidTest.apk")

# RAW capture root; screengrab nests <locale>/images/phoneScreenshots under it.
# The Play upload set is produced from these by `bun run screenshots:store`.
output_directory("fastlane/screenshots-raw")

test_instrumentation_runner("androidx.test.runner.AndroidJUnitRunner")
