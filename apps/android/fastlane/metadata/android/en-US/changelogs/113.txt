A run that hits its usage limit says so instead of going quiet: the session is marked rate limited with the time it resets, and stays steerable. The steer composer takes @ for teammates, # for issues and : for emoji. An edit's diff renders under its row, folded away until you tap it. A collapsed run of tool calls says what happened instead of counting calls. Usage bars for a second agent account on the same machine.
