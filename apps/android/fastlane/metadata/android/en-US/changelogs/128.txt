Workflows: a chip strip picks a node and opens it into Issue, Run, Changes and Results, a planner question shows in a banner, Pause and Dismiss sit in the overflow, and the final pull request merges under Reviews. Agent runs group under their workflow node with a status dot, review runs carry their titles, a run that needs you wears a dot. The blocks badge opens a mini-graph on one shared grid, a stacked run's header shows the front issue with a count. Typing --- inserts a separator.
