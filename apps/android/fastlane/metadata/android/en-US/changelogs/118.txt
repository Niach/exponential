Queued messages: a message sent while the agent is still working waits in a Queued bar above the composer, removing it puts the text back in your draft, and Stop now really interrupts the running turn. Quieter buttons keep a filled circle only for the main action, the agent picker shows the Claude Code or Codex mark, the Agent page folds past runs behind a count, signing in and choosing the active login moved onto the account chips, and issue filters are gone (search covers them).
