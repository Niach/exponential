Status, assignee, labels, board and the rest all open the same searchable sheet of plain rows, and picking several shows as a highlight instead of a circle. The list of running agent runs nests: a workflow's runs sit under one row that opens the workflow, a stack's under one row. The workflow screen drops its settings block, a node reads its own state, and the final pull request merges from there. Machine settings gain a Workflow settings page.
