Status, assignee, labels, board and the rest open one searchable sheet of plain rows; several picks show as a highlight. Running agent runs nest: a workflow's runs under one row that opens the workflow, a stack's under one row. The workflow screen drops its settings block, a node reads its own state, and the final pull request merges from there. Machine settings get a Workflow settings page. The team switcher shows a dot when another team has a live run. Uninvited members read Not invited.
