Steering v2: switch the agent's model, effort and mode from the live session with chips in the composer, and read context and usage as the run reports them. Past runs: finished sessions are listed on the machine with Resume. Chat without a repository: a chat started from the Start coding sheet no longer needs a repo. Agent picker says when a machine starts an agent in a terminal instead of the session page. Issue identifiers like #EXP-12 in the steering feed become tappable chips.
