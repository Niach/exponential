Workflows: a node whose run is live wears that run's status dot, a Running now strip opens each live run in one tap, and the issue badge in a node's sheet opens the issue. Workflows can run contract, integration and high risk nodes on their own models, and editing on the phone keeps those picks. A workflow no longer stalls when a node's pull request is merged outside of it. The Drafts tab shows again on Inbox and My Issues. Node budgets are gone.
