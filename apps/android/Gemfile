source "https://rubygems.org"

# fastlane for the Android release lanes (apps/android/fastlane). Run with
# `bundle install` then `bundle exec fastlane <lane>`. Ruby 3.x is recommended.
gem "fastlane", "~> 2.220"
